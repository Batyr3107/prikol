{
  "name": "rules-voting-app",
  "version": "1.0.0",
  "description": "Приложение для создания и голосования за правила",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "start:prod": "npx prisma migrate deploy && node src/server.js",
    "dev": "nodemon src/server.js",
    "bot": "node src/bot.js",
    "bot:dev": "nodemon src/bot.js",
    "bot:prod": "npx prisma migrate deploy && node src/bot.js",
    "both": "node scripts/start-both.js",
    "setup": "node scripts/setup.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio"
  },
  "keywords": ["voting", "rules", "telegram-bot"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "express": "^4.18.2",
    "node-telegram-bot-api": "^0.64.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "prisma": "^5.7.0",
    "nodemon": "^3.0.2",
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}
